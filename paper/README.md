# Paper

LaTeX source for the manuscript (when drafted). Suggested structure:

- `main.tex`
- `sections/` — broken-out sections for easier editing
- `bib.bib`
- `figures/` — symlinked from `../figures/` or copied

Compile with `latexmk -pdf main.tex`.
